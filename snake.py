#food import mode in skuret design path file.
### static schema for static containers and samples
    skuret_design_path = "sknuer.py"

### static schema for static containers and samples
    container_design_path = "sknuer.py"
